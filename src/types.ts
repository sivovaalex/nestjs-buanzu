export interface IUser {
    id_user: string;
    email: string
}

export interface SiteData {
    icon?: string;
    lead?: string;
    about?: string,
    gallery?: string[],
    logoFile: string,
    cssFile: string;
    htmlFile: string;
  }
  